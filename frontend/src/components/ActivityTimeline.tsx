import type { ReactNode } from 'react'
/** Reusable ActivityTimeline presentation boundary. */
export function ActivityTimeline({ children }: { children?: ReactNode }) { return <section data-component="ActivityTimeline">{children}</section> }
