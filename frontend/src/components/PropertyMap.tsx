import type { ReactNode } from 'react'
/** Reusable PropertyMap presentation boundary. */
export function PropertyMap({ children }: { children?: ReactNode }) { return <section data-component="PropertyMap">{children}</section> }
