import type { ReactNode } from 'react'
/** Reusable PropertyGallery presentation boundary. */
export function PropertyGallery({ children }: { children?: ReactNode }) { return <section data-component="PropertyGallery">{children}</section> }
