// @ts-nocheck
export function PropertyGallery({ images }) { const listingImages = images ?? []; return <article className="panel"><div className="panel-title"><span>Listing gallery</span></div>{listingImages.length ? <div className="chip-list">{listingImages.map((image) => <a key={image} href={image} target="_blank" rel="noreferrer">Listing photo ↗</a>)}</div> : <p className="muted">No listing images available.</p>}</article> }
