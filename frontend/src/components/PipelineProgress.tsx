/* eslint-disable no-undef */
// @ts-nocheck
export function PipelineProgress({ property, onRetry }: { property: Property; onRetry: () => void }) { const steps = ['Import', 'Enrichment', 'Comparables', 'Workbook Underwriting', 'Investment Memo', 'Completed']; const done = Object.values(property.pipeline_state).filter((value) => value === 'completed').length; return <section className="panel job-status"><div className="panel-title"><span>Background job status</span><button onClick={onRetry}>↻ Retry</button></div><div className="progress-bar"><i style={{ width: `${Math.max(10, done / 6 * 100)}%` }} /></div><div className="job-steps">{steps.map((step, index) => <span className={index < done ? 'done' : index === done ? 'running' : ''} key={step}>{index === done ? '◌ ' : ''}{step}</span>)}</div>{Object.keys(property.provider_errors).length > 0 && <p className="muted">Provider failures were retained; partial results are safe to review.</p>}</section> }
