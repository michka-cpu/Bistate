import type { ReactNode } from 'react'
/** Reusable PropertyComparison presentation boundary. */
export function PropertyComparison({ children }: { children?: ReactNode }) { return <section data-component="PropertyComparison">{children}</section> }
