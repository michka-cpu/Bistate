// @ts-nocheck
export function InvestmentMemo({ memo, confidence }) { return <article className="panel memo-panel"><div className="panel-title"><span>Investment memo</span><span className="confidence-pill">{Math.round(confidence ?? 0)}% confidence</span></div><p className="summary">{memo?.executive_summary ?? 'Investment memo is being prepared.'}</p><MemoList title="Strengths" items={memo?.strengths ?? []} tone="positive" /><MemoList title="Risks & weaknesses" items={[...(memo?.weaknesses ?? []), ...(memo?.risks ?? [])]} tone="warning" /></article> }
function MemoList({title,items,tone}) { return <div className={`memo-list ${tone}`}><strong>{title}</strong>{items.length ? <ul>{items.map((item) => <li key={item}>{item}</li>)}</ul> : <p className="muted">None identified yet.</p>}</div> }
